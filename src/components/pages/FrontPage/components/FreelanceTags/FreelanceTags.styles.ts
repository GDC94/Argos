import styled from "@emotion/styled";

export const Wrapper = styled.div`
  margin-top: 5rem;
  background-color: #ffd70078;
  border: 2px solid #ffd700;
  padding: 0.5rem;
  border-radius: 5px;
`;

export const AvailableTag = styled.p`
  font-size: clamp(2rem, 4vw + 5rem, 2rem);
  color: white;
  line-height: 0.8;
  cursor: default;
  font-weight: 200;
`;
