import styled from "@emotion/styled";

export const Wrapper = styled.div``;

export const AvailableTag = styled.p`
  font-size: clamp(2rem, 9vw + 5rem, 5rem);
  color: #fff;
  line-height: 0.8;
  cursor: default;
`;
