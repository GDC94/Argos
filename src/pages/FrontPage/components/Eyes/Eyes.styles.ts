import styled from "@emotion/styled";

export const Wrapper = styled.div`
  height: 300px;
  display: flex;

  align-items: flex-end;
  padding-bottom: 3rem;

  & svg {
    pointer-events: none;
  }
`;
