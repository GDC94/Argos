import { HeroTitleTag, Wrapper } from "./HeroTitle.styles";

function HeroTitle() {
  return (
    <Wrapper>
      <HeroTitleTag>I help agencies deliver web-based experiences</HeroTitleTag>
    </Wrapper>
  );
}

export default HeroTitle;
